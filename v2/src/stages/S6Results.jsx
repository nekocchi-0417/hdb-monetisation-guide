import StageStub from '../components/StageStub.jsx'

// S6 — Results (OptionCards, mechanism chain, help strip). Built in a later phase.
export default function S6Results(props) {
  return <StageStub stage="S6" titleKey="s6.title" {...props} />
}
