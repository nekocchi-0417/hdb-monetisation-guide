import StageStub from '../components/StageStub.jsx'

// S2 — Money coming in (and going out). Built in a later phase.
export default function S2Income(props) {
  return <StageStub stage="S2" titleKey="s2.title" {...props} />
}
