import StageStub from '../components/StageStub.jsx'

// S3 — The gap (goal vs income) + urgency + escalation. Built in a later phase.
export default function S3Gap(props) {
  return <StageStub stage="S3" titleKey="s3.title" {...props} />
}
